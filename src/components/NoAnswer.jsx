import Common from "./Common";

const NoAnswer = ({ a, b, c }) => {
  return (
    <div className="mt-3">
      <Common a={a} b={b} c={c} />
      <h1>No Answer</h1>
    </div>
  );
};

export default NoAnswer;
